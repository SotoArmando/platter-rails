<%= form_with(model: remotefile) do |form| %>
  <% if remotefile.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(remotefile.errors.count, "error") %> prohibited this remotefile from being saved:</h2>

      <ul>
        <% remotefile.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
